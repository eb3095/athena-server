apiVersion: v1
kind: Service
metadata:
  name: {{ include "athena-server.fullname" . }}
  labels:
    {{- include "athena-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "athena-server.selectorLabels" . | nindent 4 }}
